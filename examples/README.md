# examples/

Optional sample forged skills may live here for demos and regression tests.

Production forges always write to the user’s machine:

```text
~/.agents/skills/distilled/<slug>/
```

Install scripts never overwrite `distilled/`.
